name: FPGA CI/CD

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    # Phase 1: Scala Chisel -> Verilog
    - name: Set up Java 17
      uses: actions/setup-java@v3
      with:
        distribution: 'temurin'
        java-version: '17'
        cache: 'sbt'

    - name: Install sbt and Verilator
      run: |
        echo "deb https://repo.scala-sbt.org/scalasbt/debian all main" | sudo tee /etc/apt/sources.list.d/sbt.list
        echo "deb https://repo.scala-sbt.org/scalasbt/debian /" | sudo tee /etc/apt/sources.list.d/sbt_old.list
        curl -sL "https://keyserver.ubuntu.com/pks/lookup?op=get&search=0x2EE0EA64E40A89B84B2DF73499E82A75642AC823" | sudo apt-key add
        sudo apt-get update
        sudo apt-get install -y sbt verilator

    - name: Run Chisel Tests (Verilator Simulation)
      run: sbt test

    - name: Generate Verilog
      run: sbt "runMain blinky.BlinkyMain"

    # Phase 2: Verilog -> Bitstream (using OpenXC7 Container)
    # Reference: https://github.com/meriac/OpenXC7-LiteX

    - name: Cache Xilinx ChipDB
      id: cache-chipdb
      uses: actions/cache@v3
      with:
        path: chipdb
        key: xilinx-chipdb-xc7a35t-container

    - name: Generate Bitstream in OpenXC7 Container
      run: |
        # Create cache directory if not exists
        mkdir -p chipdb

        # Check if ChipDB exists, download if not (cache miss)
        if [ ! -f chipdb/xc7a35t.bin ]; then
          echo "Downloading xc7a35t.bin..."
          wget -q https://github.com/YosysHQ/oss-cad-suite-build/releases/download/2024-01-01/xc7a35t.bin -O chipdb/xc7a35t.bin
        fi

        # Ensure generated directory is writable by container user
        chmod -R 777 generated

        # Run make bitstream inside the container
        # We mount the chipdb directory to /opt/chipdb and override the CHIPDB_DIR variable

        docker run --rm \
          -v ${{ github.workspace }}:/workspace \
          -v ${{ github.workspace }}/chipdb:/opt/chipdb \
          -w /workspace \
          ghcr.io/meriac/openxc7-litex:latest \
          bash -l -c "make bitstream CHIPDB_DIR=/opt/chipdb"
